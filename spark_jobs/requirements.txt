# PySpark pin matching the docker runtime image (apache/spark:3.5.7).
# 3.5.7+ patches CVE-2025-54920 (Spark History UI command execution).
# Not installed in the local venv: Spark runs in the compose container (P1-11),
# and Spark-dependent tests are skipped when PySpark/Java are unavailable.
pyspark==3.5.7

# psycopg is used by the PostgreSQL sink (P1-05) for ON CONFLICT upserts.
psycopg[binary]==3.3.5

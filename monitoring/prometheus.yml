global:
  scrape_interval: 15s

scrape_configs:
  - job_name: kafka
    static_configs:
      - targets: ["kafka-exporter:9308"]   # TODO: add kafka-exporter service to compose
  - job_name: api
    static_configs:
      - targets: ["api:8000"]
